// It tells about like what is binary search tree and how to insert and search in it
// It is a binary tree in which all the nodes follow the below mentioned properties:
// The value of the left child node should be less than the value of the parent node.
// The value of the right child node should be greater than the value of the parent node.
// The Search Operation takes a LogN time as compare to N in binary tree

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */

